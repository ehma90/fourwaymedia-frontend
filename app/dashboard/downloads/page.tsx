export default function DashboardDownloadsPage() {
  return (
    <div className="mx-auto max-w-3xl space-y-3">
      <p className="text-sm leading-relaxed text-zinc-600 dark:text-zinc-400">
        Premium downloads library. In a later phase, subscribed members will access template files
        and assets here. Subscription gates access — not per-template checkout in this model.
      </p>
    </div>
  );
}
